import { TraitDescPipe } from './trait-desc.pipe';

describe('TraitDescPipe', () => {
  it('create an instance', () => {
    const pipe = new TraitDescPipe();
    expect(pipe).toBeTruthy();
  });
});
